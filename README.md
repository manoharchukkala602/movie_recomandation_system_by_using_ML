## functionality:
its can provide movie suggested  according to user likes 
its good to provide suggestion 
## features:
its have input to direct user to enter their movie name to find raleted movies suggestions
## modules:
## cosine_similarity:
its have cosine_similarity module which is import from sklearn.metrics.pairwise,
after convert the string into numerical then its give to cosine_similarity which finds the co-similur 
## function:
its main fuctionality is find co-similar in list.
## CountVectorizer:
its have Countvectorizer() which is import from sklearn.feature_extraction.text .
its can convert which am i give list of string like "tags" in this project

## functon:
its main function convert string into numerical to understand for system.
## list:
its jsut create two types of lists "movie_names" and "tags".
## movie_names:
its have all movie names and listed.
## tags:
its can listed all tags each movie.

